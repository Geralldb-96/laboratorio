// Archivo: src/archivos/settings.js
// Configuración del cliente web de archivos

const config = {
    ARCHIVOS_HOST: "localhost",
    ARCHIVOS_PORT: 5000,
    BUFFER_SIZE: 4096,
    DIRECTORIO: "/data/"
};

export default config;

